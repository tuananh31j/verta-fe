export type ICate = {
    _id: string;
    name: string;
    items: {
        _id: string;
        name: string;
    }[];
    createdAt: string;
    updatedAt: string;
};
export type ICategory = {
    _id: string;
    name: string;
};
